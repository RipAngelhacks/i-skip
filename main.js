let response = fetch(https://raw.githubusercontent.com/FuckNico/susi-code/main/main2.js);
fetch(url)
    .then(response => {
        // handle the response
    })
    .catch(error => {
        // handle the error
    });

